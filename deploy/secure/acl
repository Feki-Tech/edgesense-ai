# GENERATED by scripts/gen_broker_auth.py — do not edit by hand; regenerate instead.
#
# Registry-style per-device ACL entries (PLATFORM.md §4.2, phase 1).
# The doc's single pattern rules:
#     pattern write es/%u/sensors/#
#     pattern write es/%u/events
#     pattern read  es/%u/control
# do NOT work on mosquitto 2.x when the username contains slashes (verified
# on 2.1.2: even a device's own-prefix publish is denied), so every device
# gets an explicit block — the fallback §4.2 anticipates. Phase 2 moves this
# into the registry (dynamic-security plugin).

# device default/default/machine-01
user default/default/machine-01
topic write es/default/default/machine-01/sensors/#
topic write es/default/default/machine-01/events
topic read es/default/default/machine-01/control

# device default/default/machine-02
user default/default/machine-02
topic write es/default/default/machine-02/sensors/#
topic write es/default/default/machine-02/events
topic read es/default/default/machine-02/control

# device default/default/machine-03
user default/default/machine-03
topic write es/default/default/machine-03/sensors/#
topic write es/default/default/machine-03/events
topic read es/default/default/machine-03/control

# device acme/lyon/pump-07
user acme/lyon/pump-07
topic write es/acme/lyon/pump-07/sensors/#
topic write es/acme/lyon/pump-07/events
topic read es/acme/lyon/pump-07/control

# site gateway default/default (edge agent publishing events for the whole site)
user gw@default/default
topic write es/default/default/+/events

# ops ops (dashboards, demo tooling)
user ops
topic read es/+/+/+/events
topic write es/+/+/+/control
